input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.22.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://mysql:3306/cunewbie-search-poc"
    jdbc_user => "root"
    jdbc_password => "123456"
    tags => "to_index"
    schedule => "*/5 * * * * *"
    statement => "SELECT * FROM books WHERE updated_at > :sql_last_value AND updated_at < NOW() ORDER BY id ASC"
    #statement => "SELECT * FROM books"
  }
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.22.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://mysql:3306/cunewbie-search-poc"
    jdbc_user => "root"
    jdbc_password => "123456"
    tags => "to_delete"
    schedule => "*/5 * * * * *"
    statement => "SELECT * FROM books WHERE deleted_at > :sql_last_value AND deleted_at < NOW() ORDER BY id ASC"
  }
}
filter {
    if "to_index" in [tags] {
        mutate { add_field => { "[@metadata][action]" => "index" } }
    } else if "to_delete" in [tags] {
        mutate { add_field => { "[@metadata][action]" => "delete" } }
    }
    mutate {
        remove_field => ["@version", "@timestamp", "tags"]
    }
}


output {
 stdout { codec =>  rubydebug {metadata => true} }
 elasticsearch {
    action => "%{[@metadata][action]}"
    hosts => ["http://elasticsearch:9200"]
    index => "books"
    user => "elastic"
    password => "123456"
    document_id => "%{id}"
 }
}
